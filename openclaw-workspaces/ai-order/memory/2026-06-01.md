## TODO任务记录 (2026-06-01 16:30)

### 任务1：修复 field_transformer.py 添加 _multi_store 字段

**当前状态**: 已读取完整代码，正在修改

**修改内容**: 
在 `transform()` 函数的 `return {...}` 前插入：
```python
store_count = len(unified_stores)
is_multi = store_count > 1
```
然后在 return dict 中添加 `"_multi_store": is_multi` 和 `"_store_count": store_count`

同时需要修改 `raw_stores` 为空时的 early return，也加上 `_multi_store: False, _store_count: 0`

**文件路径**: `/Users/jinqianfei/openclaw-workspaces/ai-order/skills/skill_order_to_huading_template/tools/field_transformer.py`

**修改位置**: 
- 第122行附近：`return {` ... （需要在 return dict 中加入新字段）
- 第76-85行：early return（无 raw_stores 时）也要加字段

---

### 任务2：完整测试 execute() 流程

**测试文件**: `/Users/jinqianfei/.openclaw/media/inbound/5月27日订单---728d4ee0-01ad-40a3-a0dd-b5372e1932e3.xlsx`

**预期结果**:
- tools_parse() → 4个门店（沛县廖朵朵、邓州市廖朵朵千层蛋糕店、伊春市廖朵朵千层蛋糕、扶沟县廖朵朵）
- tools_transform() → 输出带 _multi_store=True, _store_count=4
- _match_store() → 所有4个门店匹配成功（沛县廖朵朵用关键词交叉匹配）
- _match_sku() → SKU映射
- _generate_multi_store_template() → 生成Excel

---

### 任务3：store_matcher 关键词交叉匹配（已完成）

**新增函数**: `_keyword_cross_match()` 在 Layer 3.5
- 把输入名称按2-char n-gram分词
- 对每个词搜索数据库（ILIKE '%word%'）
- 计算关键词覆盖率
- 覆盖率 >= 50% 返回匹配结果

**测试结果**: ✅ 沛县廖朵朵 → 廖朵朵-徐州沛县 (75%)