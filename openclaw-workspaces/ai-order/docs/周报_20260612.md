# AI建单助手 周报

**周期**: 2026-06-06 ~ 2026-06-12
**版本迭代**: v5.11.1 → v5.15.2（8 个版本）
**Git 提交**: 180+ commits

---

## 一、本周核心成果

### 1. Skill 核心逻辑优化（v5.11.1 → v5.15.2）

| 版本 | 修复内容 |
|------|---------|
| **v5.11.1** | 修复出库数量恒为 0 的 P0 bug（`map_sku_batch` 未回传 quantity） |
| **v5.11.2** | 多版本累积提交 + git tag 修正 + CHANGELOG 补全 |
| **v5.13.2** | 多候选 SKU 展示优化 + 中文括号正则修复 |
| **v5.13.3** | 边界字符修复：`果糖-` / `-果糖` 等末尾孤立分隔符 |
| **v5.14.0** | 单位-SKU 绑定匹配（果糖+桶不再匹配到件，准确率提升） |
| **v5.15.0** | 自学习模块 6 个缺失 EventBus.emit 补齐（事件覆盖率 40%→100%） |
| **v5.15.1** | P1 审核报告修复 + 版本一致性校验增强 |
| **v5.15.2** | store_corrected 误触发修复 + 自学习硬编码全修 + 阈值配置化 |

**准确率指标**: 82/82 = **100%**（5 维度：门店、货主、单位、仓库、SKU）

### 2. 数据库治理

- **清理**: 22 表 → 20 表（DROP `product_sku_backup` 3664行 + `shipper` 空表）
- **性能**: 新增 2 个 GIN trgm 索引（SKU 名称模糊查询加速）
- **配置统一**: 全部文档对齐为 AWS RDS，搁置 Neon 迁移方案
- **安全**: 清理 `.env` 中明文密码，恢复正确的 DB_PASSWORD

### 3. 自学习模块建设

- **事件采集**: 10 个事件订阅全部补齐 emit（之前只有 4 个）
- **分析脚本**: `analyze_learning_data.py` + `accuracy_comparison.py` + `history_replay.py`
- **配置化**: 8 处硬编码阈值 → `config/analysis_config.yaml` 统一管理
- **通知**: 飞书通知脚本路径检测 + 环境变量化
- **定时任务**: launchd plist 改为 `$AI_ORDER_WORKSPACE` 单点配置

### 4. CI 回归测试体系

- **SKU 映射回归**: 45 个测试用例（边界字符 + 端到端匹配）
- **准确率指标**: 扩展到 8 步（门店/货主/单位/仓库/SKU 五维度）
- **策略**: 改 Skill 核心代码后主动跑一遍，不集成 pre-commit / launchd

### 5. 阿里云迁移准备

- **迁移包**: v5.15.2 全量打包（198 文件，含 Skill + 自学习 + 记忆 + 文档）
- **数据库导出**: 23 表 7976 条 → SQL 文件
- **部署脚本**: `deploy_to_aliyun.sh`（首次部署）+ `auto_sync.sh`（crontab 自动同步）
- **同步脚本**: `sync_to_aliyun.sh`（本地一键 push + SSH pull + restart）
- **迁移手册**: 完整操作步骤 + 故障排查 + 验证 checklist

---

## 二、代码审计

本周完成完整代码逻辑审计（~6000 行，9 个核心文件）：

| 模块 | 文件 | 审计内容 |
|------|------|---------|
| 订单解析 | `_order_parser.py` | LLM 解析 → 原始 JSON |
| 字段标准化 | `_field_transformer.py` | 规则库标准化 |
| 门店匹配 | `_store_matcher.py` | 6 层匹配逻辑（Layer 0/1/2/3/3.5/3.6） |
| SKU 映射 | `_sku_mapper.py` | 6 层匹配逻辑（Layer 0/1/1b/2/2.5/3） |
| 模板生成 | `_template_generator.py` | 31 字段 Excel 生成 |
| 主入口 | `__init__.py` | 完整执行流 + EventBus emit 埋点 |

**发现并修复 P1 bug**: 多门店场景 + 单 confirmed_store 时，所有行使用同一门店编码（已记录，待下版本修复）

---

## 三、关键数据

| 指标 | 数值 |
|------|------|
| Skill 版本 | v5.15.2 |
| CI 测试用例 | 53 个（全部通过） |
| 准确率测试 | 82/82 = 100% |
| DB 表数 | 20 张 |
| product_sku | 1,832 条 |
| store_list | 3,327 条 |
| 货主数 | 12 个 |
| 自学习事件覆盖 | 10/10 = 100% |

---

## 四、下周计划

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P0 | 阿里云 OpenClaw 部署上线 | 迁移包已就绪，需完成部署验证 |
| P0 | 阿里云数据库导入 + 验证 | 23 表 7976 条数据迁移 |
| P1 | 多门店 confirmed_store bug 修复 | 改为 Dict[store_key, store_info] |
| P1 | Git 同步机制建立 | push → pull → restart 自动化 |
| P2 | 自学习模块实际效果验证 | 跑一周数据看学习飞轮效果 |
| P2 | A set 历史数据 GT 补全 | 9.8% 准确率问题（GT 为空 / 货主自编码） |

---

*报告生成时间: 2026-06-12 13:32 GMT+8*
